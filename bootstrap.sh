#!/bin/bash

# This script bootstraps a development environment by installing necessary packages
# and setting up configuration files.
#To run your project, navigate to the directory and run one of the following npm commands.

cd fridge
npx install
npx expo start # you need to use macOS to build the iOS project - use the Expo app if you need to do iOS development without a Mac